name: AOSP LineAgeOS SM-8750

on:
  workflow_dispatch:
    inputs:
      ksu_type:
        description: 'KernelSU Next'
        required: true
        type: choice
        default: 'ksunext'
        options:
          - 'ksunext'
          - 'none'

env:
  TZ: Asia/Shanghai

jobs:
  build:
    name: Build Kernel AOSP SM-8750 by ${{ github.actor }}
    runs-on: ubuntu-latest
    env:
      CLANG_VERSION: clang-r547379

    steps:

    - uses: actions/checkout@v6

    - name: Swap Disk Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 10

    - name: Setup Build Tools
      run: |
        echo "BUILD_TIME=$(date +%y%m%d)" >> $GITHUB_ENV
        sudo apt-get update
        sudo apt-get install -y git dwarves libelf-dev ccache automake flex bison build-essential \
          zlib1g-dev libncurses5-dev libc6-dev-i386 bc device-tree-compiler python3 make

        mkdir -p $GITHUB_WORKSPACE/kernel_workspace

    - name: Cache Clang
      uses: actions/cache@v4
      with:
        path: ${{ github.workspace }}/kernel_workspace/clang-aosp
        key: ${{ env.CLANG_VERSION }}

    - name: Download Clang
      if: steps.cache-clang.outputs.cache-hit != 'true'
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace
        mkdir -p clang-aosp
        wget -O clang.tar.gz \
          https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/${CLANG_VERSION}.tar.gz
        tar -C clang-aosp -xzf clang.tar.gz

    - name: Clone Kernel Source
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace

        git clone --recursive \
          https://github.com/LineageOS/android_kernel_oneplus_sm8750 \
          -b lineage-23.2 sm8750 --depth=1

        git clone --recursive \
          https://github.com/LineageOS/android_kernel_oneplus_sm8750-modules \
          -b lineage-23.2 sm8750-modules --depth=1

    - name: Inject Kernel Configurations
      run: |
        CONFIG="kernel_workspace/sm8750/arch/arm64/configs/vendor/oplus/sun_perf.config"

        echo "CONFIG_DODGE_DTB=y" >> $CONFIG
        echo "CONFIG_KSU=y" >> $CONFIG
        echo "CONFIG_KSU_SUSFS=y" >> $CONFIG  

    - name: Setup KernelSU-Next
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/sm8750

        if [[ "${{ github.event.inputs.ksu_type }}" == "ksunext" ]]; then

          echo "Installing KernelSU-Next..."

          curl -LSs https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/dev/kernel/setup.sh | bash -s dev || true

          if [ ! -f KernelSU-Next/kernel/Kbuild ]; then
            echo "KernelSU Incomplete, Cloning Fallback..."
            rm -rf KernelSU-Next
            git clone https://github.com/pershoot/KernelSU-Next -b dev-susfs KernelSU-Next
          fi
        
          cd KernelSU-Next

          # Sanity Check
          if [ ! -f "kernel/Makefile" ] && [ ! -f "kernel/Kbuild" ]; then
            echo "KernelSU-Next Appears Incomplete"
            exit 1
          fi

          KSU_VERSION=$(git rev-list --count HEAD)
          echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV

        else
          echo "KernelSU disabled"
        fi

    - name: Setup SUSFS
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/sm8750

        git clone --depth=1 -b gki-android15-6.6 \
          https://gitlab.com/simonpunk/susfs4ksu.git

        cp susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch KernelSU-Next/
        cp susfs4ksu/kernel_patches/50_add_susfs_in_gki-android15-6.6.patch .

        mkdir -p fs include/linux
        cp -r susfs4ksu/kernel_patches/fs/* fs/ || true
        cp -r susfs4ksu/kernel_patches/include/linux/* include/linux/ || true

        cd KernelSU-Next
        patch -p1 --forward --ignore-whitespace < 10_enable_susfs_for_ksu.patch || true

        cd ..
        patch -p1 --forward --ignore-whitespace < 50_add_susfs_in_gki-android15-6.6.patch || true

    - name: Wild Patches
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/sm8750

        git clone https://github.com/Liam314839/wildpatches

        echo "Applying Wild Patches..."

        for patch in wildpatches/*.patch; do
          echo "Applying $patch"

          if patch -p1 < "$patch"; then
            echo "OK: $patch"
          else
            echo "WARNING: Failed $patch"
          fi
        done

    - name: Verify KernelSU-Next Integrity
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/sm8750/KernelSU-Next

        echo "Checking KernelSU Compile-Critical Symbols..."
 
        # Must Exist in Codebase
        grep -R "ksu_late_loaded" kernel/ include/ . >/dev/null || {
          echo "ERROR: KSU Late Loaded Missing (Build will Fail)"
          exit 1
        }

        grep -R "ksu_avc_spoof" kernel/ include/ . >/dev/null || {
          echo "ERROR: KSU AVC Spoof Missing (Build will Fail)"
          exit 1
        }

        echo "KernelSU Core Symbols OK"

    - name: Build Kernel
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace/sm8750

        export ARCH=arm64
        export SUBARCH=arm64
        export PATH=$GITHUB_WORKSPACE/kernel_workspace/clang-aosp/bin:$PATH

        make O=out gki_defconfig vendor/sun_perf.config vendor/oplus/sun_perf.config
        make -j$(nproc) O=out

    - name: Check Image
      run: |
        test -f kernel_workspace/sm8750/out/arch/arm64/boot/Image || exit 1

    - name: AnyKernel3
      run: |
        cd $GITHUB_WORKSPACE/kernel_workspace

        git clone https://github.com/AndreiMikh/AnyKernel3
        rm -rf AnyKernel3/.git

        cp sm8750/out/arch/arm64/boot/Image AnyKernel3/

    - name: Upload
      uses: actions/upload-artifact@v7
      with:
        name: SM8750-KSU-${{ env.KSUVER }}-${{ env.BUILD_TIME }}
        path: kernel_workspace/AnyKernel3/*
